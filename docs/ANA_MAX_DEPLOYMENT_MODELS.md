# ANA MAX Deployment Models

## Local Dev

Single-machine runtime, safe-mode by default, dev-only memory and logs.

## Single-Node Service

One ANA MAX process exposes MCP/HTTP endpoints with policy gates.

## Multi-Node Cluster

Conceptual model with coordinator, workers, memory nodes, and observers.

## Hybrid Cloud/Local

Local execution and memory with optional cloud AI backends under policy.
