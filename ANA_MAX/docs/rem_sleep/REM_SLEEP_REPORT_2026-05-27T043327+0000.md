# REM Sleep Report - 2026-05-27T04:33:27+00:00

REM sleep analyzed 8 success signals, 8 friction signals, and 5 next-session rules.

## What Worked

- node --check packaged extension.js -> OK
- python ANA_MAX_Launcher/mcp_readiness_check.py --mcp-url http://127.0.0.1:8766/mcp -> OK
- Reload VS Code/Qoder if the cockpit panel was already open.
- Smart readiness is OK: `tool_router` and `agent_coach action=recommend` work.
- extension code, but did not package/reinstall/reload again during this pass.
- checkpoint button until a later reload/reinstall. The source is ready for the
- node --check vscode_extension/extension.js -> OK
- node --check ANA_MAX/extension/_vsix_unpack_103/extension/extension.js -> OK

## Mistakes Or Friction

- VS Code and Qoder CLI list commands emitted non-blocking EPERM warnings while trying to create IDE log directories, but both returned exit code 0 and showed `ana-ai.ana-antigravity-chat@1.0.4`.
- Smart readiness is OK: `tool_router` callable and `agent_coach action=recommend` returns `primary_tool=error_radar`.
- Integrated `agent_coach action=recommend` into runtime failure auto-guidance.
- Failed tool results can now include `data.auto_guidance.agent_coach_recommend`
- This makes failed tool calls directly tell the next agent what to do next
- instead of only reporting that something failed.
- Repeated failure pattern: tool_contract_validator reported 5 recent errors.
- Repeated failure pattern: router_failure_demo reported 4 recent errors.

## Patterns

- Validation-first work is paying off; keep turning successful checks into repeatable gates.
- Repeated tool errors appeared around: tool_contract_validator, router_failure_demo. Route through agent_coach before retrying.
- Friction should be converted into small durable rules, not left only in chat.
- Checkpoint memory is already useful; continue saving compact handoffs before risky transitions.

## Recommendations

- Start the next session by reading docs/NEXT_SESSION_BOOTSTRAP.md and ANA_MAX/docs/CURRENT_SESSION_HANDOFF.md.
- Run the no-reload quality gate before packaging, installing, or reloading IDE integrations.
- After two similar tool failures, call agent_coach action=recommend and follow its primary_tool.
- Turn recurring warnings or broken assumptions into conversation_learning lessons.
- Prefer tool_router for tool selection so the agent does not scan all tools blindly.

## Next Session Prompt

Before acting, read durable memory, verify MCP readiness, then follow these rules: Start the next session by reading docs/NEXT_SESSION_BOOTSTRAP.md and ANA_MAX/docs/CURRENT_SESSION_HANDOFF.md.; Run the no-reload quality gate before packaging, installing, or reloading IDE integrations.; After two similar tool failures, call agent_coach action=recommend and follow its primary_tool.; Turn recurring warnings or broken assumptions into conversation_learning lessons.
